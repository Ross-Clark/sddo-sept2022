from django.urls import path

from cyod import views


app_name= 'user'
urlpatterns = [
    #path('', views.IndexView.as_view(), name='index'),
    #path('', views.IndexView.as_view(), name='index'),
    #path('', views.IndexView.as_view(), name='index'),
]